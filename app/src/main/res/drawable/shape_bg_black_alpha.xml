<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="30dp"/>   <!-- 设置圆角弧度 -->
    <solid android:color="@color/black_alpha"/>   <!-- 设置背景颜色 -->
    <padding
        android:left="6dp"
        android:right="6dp"
        />
</shape>